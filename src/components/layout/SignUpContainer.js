import styled from "styled-components";
import { SignUpContainerStyles } from "../../styles/common";

const SignUpContainer = styled.section`
  ${SignUpContainerStyles}
`;

export default SignUpContainer;
